
Q1 – A. 6.5

Q2 - C. First line:
        Last line: ooooooooo

Q3 - D. Exit condition

Q4 - C. It prints a message to the console

Q5 - D. new_list = [ ]
           for course in courses:
               if course.contains(“cfg”):
                    new_list.append(course)

Q6 - D. Returns the sum of all values in the specified column

Q7 - B. LEFT JOIN

Q8 - B. SELECT * FROM employees WHERE department == “sales” AND salary > 50000;

Q9 - D. SELECT student_name FROM students JOIN courses ON students.student_id = courses.student_id WHERE courses.course_id IS NULL;

Q10 - A. SELECT MIN(salary) FROM employees WHERE salary > 50000;
